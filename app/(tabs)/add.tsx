// Placeholder screen for the center (+) tab button.
// This screen is never actually shown — the tab button navigates to /add-item instead.
// Expo Router requires a file to exist for each Tabs.Screen name.

import { View } from "react-native";

export default function AddPlaceholder() {
  return <View />;
}
